This is a simple demonstration of a Docker Desktop problem with ownership of a subdirectory of a mount.

To run it:

```bash
git clone https://github.com/rfay/dd_ownership_test && cd dd_ownership_test
./demoproblem.sh
```
